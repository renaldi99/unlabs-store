import { addClass, removeClass } from "./utils-class";
import Typed from "typed.js";

var typed = new Typed(".type-animate", {
  strings: ["aesthetic", "mewah", "gaming?"],
  typeSpeed: 100,
  backSpeed: 50,
  loop: true,
  startDelay: 1000,
});
